{
  "name": "greek-reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "python3 scripts/build-search-index.py && python3 scripts/build_grc_index.py && vite build",
    "preview": "vite preview",
    "postbuild": "node scripts/postbuild.mjs",
    "deploy:vercel": "npx -y vercel@latest deploy dist --prod --yes",
    "build:generic": "DEPLOY_TARGET=generic npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "espeak-ng": "^1.0.2",
    "playwright-core": "^1.49.0"
  }
}
